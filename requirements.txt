python-dotenv
python-docx
langchain-text-splitters
chromadb
sentence-transformers
tiktoken
rank_bm25
groq
streamlit

# Per-answer RAGAS-style scoring (llm_metrics.py) adds NO dependencies. It is
# a single judge-LLM call over the `groq` client already listed above.
#
# `ragas` and `langchain-groq` used to be here. They pull datasets + pyarrow +
# langchain-core transitively, the install exceeded what a Streamlit Community
# Cloud container will take, and pip failed while the app still booted - which
# is exactly how the UI ended up printing "ragas unavailable
# (ModuleNotFoundError)" on every answer. Do not add them back.
